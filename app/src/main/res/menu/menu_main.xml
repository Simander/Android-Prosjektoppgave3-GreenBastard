<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">
    <item android:id="@+id/newgame_mbutton"
        android:icon="@drawable/restart5"
        android:title="New Game"
        app:showAsAction="always"
        />
    <item android:id="@+id/play_mbutton"
        android:icon="@drawable/pause"
        android:title="Play"
        app:showAsAction="always"
        />
    <item android:id="@+id/highscore_mbutton"
        android:icon="@drawable/highscore"
        android:title="Highscore"
        app:showAsAction="always"
        />

    <item android:id="@+id/help_mbutton"
        android:icon="@drawable/help"
        android:title="Help"
        app:showAsAction="always"/>
    <item android:id="@+id/quit_mbutton"
        android:icon="@drawable/quit"
        android:title="Quit"
        app:showAsAction="always"/>
</menu>
